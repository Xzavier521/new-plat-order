<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nsw.wx.order.mapper.UserMapper" >
  <resultMap id="BaseResultMap" type="com.nsw.wx.order.pojo.User" >
    <id column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="user_name" property="userName" jdbcType="VARCHAR" />
    <result column="password" property="password" jdbcType="VARCHAR" />
    <result column="phone" property="phone" jdbcType="VARCHAR" />
  </resultMap>

  <insert id="insert" parameterType="com.nsw.wx.order.pojo.User" >
    insert into t_user (user_id, user_name, password, 
      phone)
    values (#{userId,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, 
      #{phone,jdbcType=VARCHAR})
  </insert>

  <sql id="base_query">
    <where>
      <if test="beginDate!=null">
        AND loginTime &gt;=#{beginDate}
      </if>
      <if test="endDate!=null">
        AND loginTime &lt;=#{endDate}
      </if>
      <if test="username!=null and like">
        AND username like concat('%',#{username},'%')
      </if>
      <if test="username!=null and !like">
        AND username =#{username}
      </if>
      <if test="userType>-1">
        AND loginType =#{userType}
      </if>
      <if test="state >-1">
        AND loginstate = #{state}
      </if>
    </where>
  </sql>

  <select id = "queryAll" resultMap="BaseResultMap">
    select  * from  t_user
  </select>

  <delete id="delectTest" parameterType="int">
    delete from t_user where user_id=#{user_id}
  </delete>
</mapper>